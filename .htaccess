# MAMP .htaccess Configuration
# Enable URL rewriting for clean URLs

RewriteEngine On
RewriteBase /

# Redirect index.php to root first (single redirect)
RewriteCond %{THE_REQUEST} \s/index\.php[\s?] [NC]
RewriteRule ^index\.php$ / [R=301,L]

# 301 redirect all other .php URLs to clean URLs (so Google indexes only canonical URLs)
RewriteCond %{THE_REQUEST} \s/[^.]+\.php [NC]
RewriteRule ^(.+)\.php$ /$1 [R=301,L]

# Redirect /index to root
RewriteCond %{REQUEST_URI} ^/index/?$
RewriteRule ^(.*)$ / [R=301,L]

# Clean URLs - internally rewrite /page to page.php
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^([^\.]+)$ $1.php [NC,L]

# Custom Error Pages
ErrorDocument 404 /index.php
ErrorDocument 403 /index.php
ErrorDocument 500 /index.php

# SEO redirects
RewriteCond %{REQUEST_URI} ^/home/?$
RewriteRule ^(.*)$ / [R=301,L]

RewriteCond %{REQUEST_URI} ^/contact-us/?$
RewriteRule ^(.*)$ /contact [R=301,L]

RewriteCond %{REQUEST_URI} ^/our-services/?$
RewriteRule ^(.*)$ /services [R=301,L]

RewriteCond %{REQUEST_URI} ^/about-us/?$
RewriteRule ^(.*)$ /about [R=301,L]

# Set default charset
AddDefaultCharset UTF-8